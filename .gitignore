node_modules/
.pnpm-store/
.wireit/
.svelte-kit/
build/
build.live/
build.live.tmp/
build.prev/
build.broken/
dist/
# We use pnpm; reject stray npm/yarn lockfiles.
package-lock.json
yarn.lock
.env
.env.*
!.env.example
data/
*.db
*.db-journal
*.db-wal
*.db-shm
*.log
.DS_Store
playwright-report/
test-results/
.generated/
# Playwright CLI (browser automation) scratch: snapshots, console logs, traces.
.playwright-cli/
coverage/
# ZAP's per-project dir self-ignores its ephemeral scratch tree via a
# generated .zap/.gitignore; no blanket .zap/ entry so committed config
# under .zap/ stays trackable.
